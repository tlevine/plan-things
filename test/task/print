. ../../plan
DAYS=0.25
MESSAGE=abc

expected='--------
abc

(0.25 days)'

test "$(task_print)" = "$expected"
result=$?

if test $result -ne 0; then
  task_print > /tmp/observed
  echo "$expected" > /tmp/expected
  diff /tmp/{observed,expected}
  exit $result
fi
