task_print() {
  echo '--------'
  echo "$MESSAGE"
  echo
  echo "($DAYS days)"
}

thing_print() {
  echo '========'
  echo "$MESSAGE"
  echo
  echo "($DAYS days)"
}

thing_total_days() {
  expression=$(grep -r -m1 '^DAYS=' "$1" | cut -d= -f2 | tr '\n' + | sed 's/+$//')
  echo $expression | bc
}
